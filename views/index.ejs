<!DOCTYPE html>
<html>
<head>
  <title>Safe Todo App</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<h1>✅ My Todo List</h1>

<form action="/add" method="POST">
  <input type="text" name="text" placeholder="Add a task..." required maxlength="200">
  <button>Add</button>
</form>

<ul>
  <% todos.forEach(todo => { %>
    <li class="<%= todo.completed ? 'done' : '' %>">
      <form action="/toggle/<%= todo._id %>" method="POST">
        <button>✔</button>
      </form>

      <span><%= todo.text %></span>

      <form action="/delete/<%= todo._id %>" method="POST">
        <button>🗑</button>
      </form>
    </li>
  <% }) %>
</ul>

</body>
</html>
